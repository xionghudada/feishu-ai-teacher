name: AI 老师自动批改作业

on:
  workflow_dispatch: # 允许手动触发
  
  schedule:
    # =======================================================
    # 🗓️ 场景一：周一至周五 (Mon-Fri)
    # 目标：北京时间 17:40 - 22:30 (每 15 分钟一次)
    # UTC 转换: -8 小时
    # =======================================================
    
    # 1.1 启动阶段：北京 17:40, 17:55
    # (UTC 09:40, 09:55)
    - cron: '40,55 9 * * 1-5'
    
    # 1.2 中间主力阶段：北京 18:00 - 21:59 (整点、15、30、45)
    # (UTC 10:00 - 13:59)
    - cron: '*/15 10-13 * * 1-5'
    
    # 1.3 收尾阶段：北京 22:00, 22:15, 22:30
    # (UTC 14:00, 14:15, 14:30)
    - cron: '0,15,30 14 * * 1-5'

    # =======================================================
    # 🗓️ 场景二：周末 (Sat, Sun)
    # 目标：北京时间 09:00 - 22:00 (每 30 分钟一次)
    # UTC 转换: -8 小时
    # =======================================================
    
    # 2.1 主力阶段：北京 09:00 - 21:30
    # (UTC 01:00 - 13:30)
    # 注：UTC 13点对应北京21点，覆盖到 21:30
    - cron: '*/30 1-13 * * 6,0'
    
    # 2.2 收尾阶段：北京 22:00 整点
    # (UTC 14:00)
    - cron: '0 14 * * 6,0'

jobs:
  grade_homework:
    runs-on: ubuntu-latest
    
    steps:
    - name: 拉取代码
      uses: actions/checkout@v3
      
    - name: 设置 Python 环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: 安装依赖库
      run: |
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: 运行批改脚本
      env:
        APP_ID: ${{ secrets.APP_ID }}
        APP_SECRET: ${{ secrets.APP_SECRET }}
        APP_TOKEN: ${{ secrets.APP_TOKEN }}
        TABLE_ID: ${{ secrets.TABLE_ID }}
        AI_API_KEY: ${{ secrets.AI_API_KEY }}
      run: python main.py
