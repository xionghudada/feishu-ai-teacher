name: 每天定时清空表格 (06:00版)

on:
  # 允许手动点击按钮触发
  workflow_dispatch:
  
  # 定时任务
  schedule:
    # =======================================================
    # 🗓️ 每天 (Daily)
    # 目标：北京时间 06:00 (清晨)
    # UTC 转换: 北京 06:00 - 8小时 = UTC 22:00 (前一天)
    # 作用：在早晨 9 点批改开始前，提前 3 小时清空旧数据
    # =======================================================
    - cron: '0 22 * * *'

jobs:
  clear_data:
    runs-on: ubuntu-latest
    
    steps:
    - name: 拉取代码
      uses: actions/checkout@v3
      
    - name: 设置 Python 环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: 安装依赖库
      run: pip install lark-oapi
      
    - name: 执行清空脚本
      env:
        APP_ID: ${{ secrets.APP_ID }}
        APP_SECRET: ${{ secrets.APP_SECRET }}
        APP_TOKEN: ${{ secrets.APP_TOKEN }}
        TABLE_ID: ${{ secrets.TABLE_ID }}
      run: python clear_data.py
